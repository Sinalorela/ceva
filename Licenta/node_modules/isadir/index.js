'use strict'
// node module loads
let fs    = require('fs');

// main function
let isadir = (fname) => {
    // is filename directory?
    try { return fs.statSync(fname).isDirectory();} 

    // file or directory not exist
    catch (e) { return false; } 
}   

// export module
module.exports = isadir;